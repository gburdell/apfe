#!/bin/csh -f

set cmd=$0
if (1 > `echo $cmd | egrep -c '^/'`) then
	set cmd=$PWD/${cmd}
endif
set rdir=$cmd:h:h

set cls="apfe.dsl.vlogpp.Main"
exec java -Xms2G -ea -Dtool.root=${rdir} -cp ${rdir}/dist/apfe.jar ${cls} ${*}
