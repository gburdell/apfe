#!/bin/csh -f

set cmd=$0
if (1 > `echo $cmd | egrep -c '^/'`) then
	set cmd=$PWD/${cmd}
endif
set rdir=$cmd:h:h

set cls="apfe.dsl.vlogpp.Main"
set jars=${rdir}/dsl/dist/lib/apfe.jar:${rdir}/dsl/dist/dsl.jar
exec java -Xms2G -ea -Dtool.root=${rdir} -cp ${jars} ${cls} ${*}
